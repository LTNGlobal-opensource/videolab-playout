# Variable configurables used for testing

CASENAME="Case 338 - 1920x1080p59.94 - SDI - 2min NBA no audio (burnwriter)"

# GENERIC_PLAYOUT_TYPE (optional, but upper case mandatory) options:
#  NONE
#  IP_TS
#  SDI
#  NDI (unsupported)
#  PCAP (unsupported)
GENERIC_PLAYOUT_TYPE=SDI

GENERIC_PLAYOUT_APP=decklink-generic-decode-to-sdi.sh
GENERIC_ARGS=" --input $VPBASE/streams/1920x1080p5994-nba-tissot-ac3-2min-burnwriter-x264.mp4"
GENERIC_ARGS="$GENERIC_ARGS --enableallaudio"
