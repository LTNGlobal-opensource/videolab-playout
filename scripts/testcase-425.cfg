# Variable configurables used for testing

CASENAME="Case 425 - 1920x1080i25    - IP  - BBC Master feed with Teletext WST present NO PMT descriptor (4mins)"

# GENERIC_PLAYOUT_TYPE (optional, but upper case mandatory) options:
#  NONE
#  IP_TS
#  SDI
#  NDI (unsupported)
#  PCAP (unsupported)
GENERIC_PLAYOUT_TYPE=IP_TS

GENERIC_PLAYOUT_APP=ip-generic-mpts-to-ip.sh
GENERIC_ARGS="$GENERIC_ARGS --input $VPBASE/streams/1920x1080i25-bbc-news-4min.ts"
GENERIC_ARGS="$GENERIC_ARGS --pcr-pid 0x31"
